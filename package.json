{
  "name": "softlaunch_net",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel ./*.html",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url https://softlaunch.net && cp CNAME ./dist",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.15.4"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.15.4",
    "@parcel/transformer-webmanifest": "^2.15.4"
  }
}
